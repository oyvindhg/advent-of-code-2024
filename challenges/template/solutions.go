package main

import (
	"fmt"
)

func solveFirst(input []string) int {
	for _, row := range input {
		fmt.Println(row)
	}

	return 0
}

func solveSecond(input []string) int {
	for _, row := range input {
		fmt.Println(row)
	}
	return 0
}
